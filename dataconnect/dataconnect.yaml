specVersion: "v1beta"
serviceId: "twilio-call-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "twilio-call-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
